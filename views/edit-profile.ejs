<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.min.css">
    <style>
        /* Kontener główny profilu z flexboxem */
        .profile-container {
            display: flex;
            align-items: flex-start;
            margin-bottom: 40px;
        }
        /* Kontener dla avatara i przycisków działających na awatar */
        .avatar-container {
            margin-right: 40px;
        }
        /* Stylizacja avatara użytkownika */
        .avatar {
            width: 100px;
            height: 100px;
            background-color: #ddd;
            border-radius: 50%;
            display: block;
            margin-bottom: 15px;
        }
        /* Przycisk do zmiany avatara */
        .change-avatar-button {
            display: block;
            margin-bottom: 30px;
            text-align: center;
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
        }
        /* Etykiety dla informacji o profilu użytkownika */
        .profile-label {
            font-weight: bold;
            margin-bottom: 5px;
        }
        /* Wartości dla informacji o profilu użytkownika */
        .profile-value {
            margin-bottom: 20px; /* Odstęp między informacjami */
        }
        /* Link do powrotu na stronę główną */
        .return-link {
            display: block;
            margin-top: 20px;
            font-size: 16px;
            text-decoration: none;
            color: blue;
        }
    </style>
    <title>Edycja Profilu</title>
</head>
<body>
    <!-- Główny kontener dla elementów profilu -->
    <div class="profile-container">
        <!-- Kontener dla avatara i przycisków akcji -->
        <div class="avatar-container">
            <!-- Avatar użytkownika -->
            <div class="avatar"></div>
            <!-- Przycisk do zmiany avatara -->
            <a href="/users/dashboard" class="change-avatar-button">Zmień awatar</a>
            <!-- Link do powrotu na stronę główną dashboardu -->
            <a href="/users/dashboard" class="return-link">Powrót do strony głównej</a>
        </div>
        <!-- Kontener dla szczegółów profilu -->
        <div>
            <!-- Nick użytkownika -->
            <div class="profile-label">Nick:</div>
            <div class="profile-value"><%= user.name %></div>
            <!-- Email użytkownika -->
            <div class="profile-label">Email:</div>
            <div class="profile-value"><%= user.email %></div>
        </div>
        <form action="/users/dashboard/edit" method="POST">
            <label for="name">Nick:</label>
            <input type="text" id="newName" name="newName" >
            <button type="submit">Zapisz zmiany</button>
        </form>
        
        
        
    </div>
</body>
</html>
